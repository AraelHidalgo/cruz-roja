# Cruz Roja Mockup

## Chatbot con Gemini

1. Crea una llave en Google AI Studio.
2. Copia `.env.example` a `.env.local`.
3. Agrega tu llave en `GEMINI_API_KEY`.
4. Reinicia el servidor de desarrollo.

El modelo por defecto es `gemini-3.5-flash`. Puedes cambiarlo con `GEMINI_MODEL`.
